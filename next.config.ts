import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  images: {
    // Add any external image domains here when you source real product images
    remotePatterns: [],
  },
};

export default nextConfig;
